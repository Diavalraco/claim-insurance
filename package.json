{
  "name": "claim-checker",
  "version": "1.0.0",
  "private": true,
  "description": "US Insurance Claim Fraud Detector",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "preview": "cd frontend && npm run preview"
  },
  "dependencies": {
    "openai": "^4.104.0"
  }
}
